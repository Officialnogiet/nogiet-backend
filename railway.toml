[build]
  buildCommand = "npm install"

[deploy]
  startCommand = "npm run db:migrate && npx tsx src/server.ts"
  # Health check
  healthcheckPath = "/health"
  healthcheckTimeout = 60
  # Restart on failure
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
